# Allow keystore to find gatekeeper's shared secret service
# This is a vendor-specific service type
allow keystore gatekeeper_sharedsecret_service:service_manager find;

# Allow keystore to call gatekeeper via binder for secure operations
allow keystore hal_gatekeeper_default:binder call;
