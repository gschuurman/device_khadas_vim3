# Allow mediaswcodec to find graphics mapper service
allow mediaswcodec hal_graphics_mapper_service:service_manager find;

# Allow reading vendor graphics properties
get_prop(mediaswcodec, vendor_graphics_prop)

# Suppress audits for vendor_default_prop access (Treble compliance)
dontaudit mediaswcodec vendor_default_prop:file { read open getattr map };
